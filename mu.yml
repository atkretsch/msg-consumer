environments:
- name: acceptance
- name: production
service:
  port: 80
  pathPatterns:
  - /*
  pipeline:
    source:
      provider: GitHub
      repo: atkretsch/msg-consumer
templates:
  mu-iam-service-msg-consumer-acceptance:
    Resources:
      EcsTaskRole:
        Properties:
          ManagedPolicyArns:
          - "arn:aws:iam::aws:policy/AmazonSQSFullAccess"
